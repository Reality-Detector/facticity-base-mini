// Export all shadcn/ui components from this index
// This allows for clean imports like: import { Button, Input } from '@/components/ui'

// Components
export { Button, buttonVariants } from './button';
